# Draft Content

This folder preserves portfolio work that is not part of the public launch surface.

- `work/sendmoi/` keeps the SendMoi case study draft.

## SendMoi draft notes

- The current SendMoi draft is being refined as a long-form case study.
- Review path during local preview: `/drafts/work/sendmoi/`.
- The draft currently uses a cache-busted stylesheet URL so phone Safari picks up active CSS edits during review.
- SendMoi-specific setup/autosend image sizing is a temporary draft override. Move this into a reusable block/media presentation option when the CMS/block system exists.

The deploy script does not copy this directory, and public pages should not link here.
