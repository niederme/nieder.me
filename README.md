# nieder.me

## Local dev

Run this from the repo root:

```bash
make
```

That starts a static server at `http://localhost:8000` and opens the site in your browser automatically.

Use a different port if needed:

```bash
make dev PORT=8080
```
