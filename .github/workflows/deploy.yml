name: Deploy Staging

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Check social metadata
        run: ./scripts/check-social-metadata.py

      - name: Minify CSS and JS
        run: |
          npm install -g lightningcss-cli terser
          lightningcss --minify --sourcemap assets/css/styles.css -o assets/css/styles.css
          lightningcss --minify --sourcemap assets/css/work-case-study.css -o assets/css/work-case-study.css
          lightningcss --minify --sourcemap assets/css/styleguide.css -o assets/css/styleguide.css
          terser assets/js/main.js -o assets/js/main.js --sourcemap "url='main.js.map'" --compress --mangle

      - name: Refresh sitemap
        run: ./scripts/update-sitemap.py

      - name: Set up SSH
        run: |
          install -m 700 -d ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p 22 ssh.suckahs.org >> ~/.ssh/known_hosts
          cat >> ~/.ssh/config <<'EOF'
          Host ssh.suckahs.org
            IdentityFile ~/.ssh/deploy_key
            StrictHostKeyChecking yes
          EOF

      - name: Deploy
        run: ./scripts/deploy-2026.sh
